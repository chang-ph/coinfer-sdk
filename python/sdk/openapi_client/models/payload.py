# coding: utf-8

"""
    Coinfer Server API

     This part of document describes the HTTP APIs of the Coinfer cloud.  

    The version of the OpenAPI document: 0.0.1749800012+14d5928
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from openapi_client.models.list_event import ListEvent
from openapi_client.models.list_experiment import ListExperiment
from openapi_client.models.list_model import ListModel
from openapi_client.models.list_relation import ListRelation
from openapi_client.models.list_share import ListShare
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

PAYLOAD_ONE_OF_SCHEMAS = ["ListEvent", "ListExperiment", "ListModel", "ListRelation", "ListShare"]

class Payload(BaseModel):
    """
    Payload
    """
    # data type: ListModel
    oneof_schema_1_validator: Optional[ListModel] = None
    # data type: ListExperiment
    oneof_schema_2_validator: Optional[ListExperiment] = None
    # data type: ListEvent
    oneof_schema_3_validator: Optional[ListEvent] = None
    # data type: ListRelation
    oneof_schema_4_validator: Optional[ListRelation] = None
    # data type: ListShare
    oneof_schema_5_validator: Optional[ListShare] = None
    actual_instance: Optional[Union[ListEvent, ListExperiment, ListModel, ListRelation, ListShare]] = None
    one_of_schemas: Set[str] = { "ListEvent", "ListExperiment", "ListModel", "ListRelation", "ListShare" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Payload.model_construct()
        error_messages = []
        match = 0
        # validate data type: ListModel
        if not isinstance(v, ListModel):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ListModel`")
        else:
            match += 1
        # validate data type: ListExperiment
        if not isinstance(v, ListExperiment):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ListExperiment`")
        else:
            match += 1
        # validate data type: ListEvent
        if not isinstance(v, ListEvent):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ListEvent`")
        else:
            match += 1
        # validate data type: ListRelation
        if not isinstance(v, ListRelation):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ListRelation`")
        else:
            match += 1
        # validate data type: ListShare
        if not isinstance(v, ListShare):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ListShare`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Payload with oneOf schemas: ListEvent, ListExperiment, ListModel, ListRelation, ListShare. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Payload with oneOf schemas: ListEvent, ListExperiment, ListModel, ListRelation, ListShare. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ListModel
        try:
            instance.actual_instance = ListModel.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ListExperiment
        try:
            instance.actual_instance = ListExperiment.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ListEvent
        try:
            instance.actual_instance = ListEvent.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ListRelation
        try:
            instance.actual_instance = ListRelation.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ListShare
        try:
            instance.actual_instance = ListShare.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Payload with oneOf schemas: ListEvent, ListExperiment, ListModel, ListRelation, ListShare. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Payload with oneOf schemas: ListEvent, ListExperiment, ListModel, ListRelation, ListShare. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], ListEvent, ListExperiment, ListModel, ListRelation, ListShare]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


